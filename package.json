{
  "type": "module",
  "name": "qr-code-generator",
  "version": "1.0.0",
  "description": "this project generates a qr code for the website given when \"node index.js\" command run from the terminal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sushant Soma",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^9.2.10",
    "qr-image": "^3.2.0"
  }
}
